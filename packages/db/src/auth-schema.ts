// Supabase Auth manages its own auth.users table — no Drizzle schema needed here.
// Application user data (patient profiles, etc.) will be added to packages/db/src/schema/
// in later Epic 0 and Epic 1 stories.
