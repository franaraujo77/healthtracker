{
  "name": "@healthtracker/config",
  "version": "0.1.0",
  "private": true,
  "exports": {
    ".": "./src/sentry.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:unit": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@healthtracker/tsconfig": "workspace:*",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
