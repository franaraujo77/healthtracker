"use client";

import { styled, Label as TamaguiLabel } from "tamagui";

export const Label = styled(TamaguiLabel, {
  name: "Label",
  fontFamily: "$body",
  color: "$textPrimary",
  fontSize: "$4",
  fontWeight: "500",
});
